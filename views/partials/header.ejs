<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title || 'Hostel Booking System' %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
<header class="site-header">
  <nav class="container nav">
    <a class="brand" href="/">CampusStay</a>
    <div class="nav-links">
      <% if (user) { %>
        <a href="/dashboard">Dashboard</a>
        <a href="/profile">My profile</a>
        <form action="/logout" method="post"><button class="link-button" type="submit">Log out</button></form>
      <% } else { %>
        <a href="/login">Log in</a>
        <a class="button" href="/register">Create account</a>
      <% } %>
    </div>
  </nav>
</header>
<main>
